# Supabase credentials — copy this file to .env and fill in your values.
# NEVER commit .env to git. It is already in .gitignore.
#
# 1. Go to https://supabase.com → your project → Settings → API
# 2. Copy the "Project URL" and "anon public" key below.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# For Vercel: add these as Environment Variables in the Vercel dashboard
# (Settings → Environment Variables) — do NOT add them to vercel.json.
